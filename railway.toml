[build]
builder = "railpack"

[build.env]
RAILPACK_PHP_EXTENSIONS = "gd"

[deploy]
startCommand = "php artisan serve --host=0.0.0.0 --port=${PORT:-8080}"

[deploy.env]
DB_CONNECTION = "sqlite"
DB_DATABASE = ":memory:"
